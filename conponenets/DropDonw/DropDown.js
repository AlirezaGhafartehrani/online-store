// define Elements:

const logOutBtn = document.getElementById("logOutBtn");

// event listeners

logOutBtn.addEventListener("click", function logout(e) {
  // logout and navigate some where else
});
